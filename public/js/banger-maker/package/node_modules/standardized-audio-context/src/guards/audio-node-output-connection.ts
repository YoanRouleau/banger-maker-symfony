import { TAudioNodeOutputConnection, TContext, TOutputConnection } from 'standardized-audio-context/src/types/index';
import { isAudioNode } from 'standardized-audio-context/src/guards/audio-node';

export const isAudioNodeOutputConnection = <T extends TContext>(
    outputConnection: TOutputConnection<T>
): outputConnection is TAudioNodeOutputConnection<T> => {
    return isAudioNode(outputConnection[0]);
};
