import { ACTIVE_AUDIO_NODE_STORE } from 'standardized-audio-context/src/globals';
import { TIsActiveAudioNodeFunction } from 'standardized-audio-context/src/types/index';

export const isActiveAudioNode: TIsActiveAudioNodeFunction = (audioNode) => ACTIVE_AUDIO_NODE_STORE.has(audioNode);
