import { CYCLE_COUNTERS } from 'standardized-audio-context/src/globals';
import { TIsPartOfACycleFunction } from 'standardized-audio-context/src/types/index';

export const isPartOfACycle: TIsPartOfACycleFunction = (audioNode) => {
    return CYCLE_COUNTERS.has(audioNode);
};
