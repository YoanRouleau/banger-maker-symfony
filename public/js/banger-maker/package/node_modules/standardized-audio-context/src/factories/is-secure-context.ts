import { TIsSecureContextFactory } from 'standardized-audio-context/src/types/index';

export const createIsSecureContext: TIsSecureContextFactory = (window) => window !== null && window.isSecureContext;
