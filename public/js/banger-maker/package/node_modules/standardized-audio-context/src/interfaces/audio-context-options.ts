import { TAudioContextLatencyCategory } from 'standardized-audio-context/src/types/index';

export interface IAudioContextOptions {
    latencyHint?: number | TAudioContextLatencyCategory;

    sampleRate?: number;
}
