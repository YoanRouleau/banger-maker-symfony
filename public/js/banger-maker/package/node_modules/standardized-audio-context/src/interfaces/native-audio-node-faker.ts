import { TNativeAudioNode } from 'standardized-audio-context/src/types/index';

export interface INativeAudioNodeFaker extends TNativeAudioNode {
    bufferSize?: number;

    inputs: TNativeAudioNode[];
}
