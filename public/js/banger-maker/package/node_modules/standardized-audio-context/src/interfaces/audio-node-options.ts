import { TChannelCountMode, TChannelInterpretation } from 'standardized-audio-context/src/types/index';

export interface IAudioNodeOptions {
    channelCount: number;

    channelCountMode: TChannelCountMode;

    channelInterpretation: TChannelInterpretation;
}
