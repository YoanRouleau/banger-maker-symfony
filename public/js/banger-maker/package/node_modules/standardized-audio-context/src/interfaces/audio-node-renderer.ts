import { TNativeAudioNode, TNativeOfflineAudioContext } from 'standardized-audio-context/src/types/index';
import { IAudioNode } from 'standardized-audio-context/src/interfaces/audio-node';
import { IMinimalOfflineAudioContext } from 'standardized-audio-context/src/interfaces/minimal-offline-audio-context';
import { IOfflineAudioContext } from 'standardized-audio-context/src/interfaces/offline-audio-context';

export interface IAudioNodeRenderer<T extends IMinimalOfflineAudioContext | IOfflineAudioContext, U extends IAudioNode<T>> {
    render(proxy: U, nativeOfflineAudioContext: TNativeOfflineAudioContext, trace: readonly IAudioNode<T>[]): Promise<TNativeAudioNode>;
}
