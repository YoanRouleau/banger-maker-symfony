import { IStereoPannerNode, IStereoPannerOptions } from 'standardized-audio-context/src/interfaces/index';
import { TContext } from 'standardized-audio-context/src/types/context';

export type TStereoPannerNodeConstructor = new <T extends TContext>(
    context: T,
    options?: Partial<IStereoPannerOptions>
) => IStereoPannerNode<T>;
