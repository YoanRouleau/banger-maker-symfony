import { IAudioBufferSourceNode, IAudioBufferSourceOptions } from 'standardized-audio-context/src/interfaces/index';
import { TContext } from 'standardized-audio-context/src/types/context';

export type TAudioBufferSourceNodeConstructor = new <T extends TContext>(
    context: T,
    options?: Partial<IAudioBufferSourceOptions>
) => IAudioBufferSourceNode<T>;
