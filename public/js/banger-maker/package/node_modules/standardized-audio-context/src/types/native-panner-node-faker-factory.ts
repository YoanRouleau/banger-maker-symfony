import { INativePannerNodeFaker, IPannerOptions } from 'standardized-audio-context/src/interfaces/index';
import { TNativeContext } from 'standardized-audio-context/src/types/native-context';

export type TNativePannerNodeFakerFactory = (nativeContext: TNativeContext, options: IPannerOptions) => INativePannerNodeFaker;
