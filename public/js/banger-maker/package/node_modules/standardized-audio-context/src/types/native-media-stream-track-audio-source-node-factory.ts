import { IMediaStreamTrackAudioSourceOptions } from 'standardized-audio-context/src/interfaces/index';
import { TNativeAudioContext } from 'standardized-audio-context/src/types/native-audio-context';
import { TNativeMediaStreamTrackAudioSourceNode } from 'standardized-audio-context/src/types/native-media-stream-track-audio-source-node';

export type TNativeMediaStreamTrackAudioSourceNodeFactory = (
    nativeAudioContext: TNativeAudioContext,
    options: IMediaStreamTrackAudioSourceOptions
) => TNativeMediaStreamTrackAudioSourceNode;
