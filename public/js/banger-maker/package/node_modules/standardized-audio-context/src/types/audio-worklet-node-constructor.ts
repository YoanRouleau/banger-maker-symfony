import { IAudioWorkletNode, IAudioWorkletNodeOptions } from 'standardized-audio-context/src/interfaces/index';
import { TContext } from 'standardized-audio-context/src/types/context';

export type TAudioWorkletNodeConstructor = new <T extends TContext>(
    context: T,
    name: string,
    options?: Partial<IAudioWorkletNodeOptions>
) => IAudioWorkletNode<T>;
