import { IAudioWorkletNodeOptions, IAudioWorkletProcessorConstructor } from 'standardized-audio-context/src/interfaces/index';
import { TNativeAudioWorkletNode } from 'standardized-audio-context/src/types/native-audio-worklet-node';
import { TNativeAudioWorkletNodeConstructor } from 'standardized-audio-context/src/types/native-audio-worklet-node-constructor';
import { TNativeContext } from 'standardized-audio-context/src/types/native-context';

export type TNativeAudioWorkletNodeFactory = (
    nativeContext: TNativeContext,
    baseLatency: null | number,
    nativeAudioWorkletNodeConstructor: null | TNativeAudioWorkletNodeConstructor,
    name: string,
    processorConstructor: undefined | IAudioWorkletProcessorConstructor,
    options: IAudioWorkletNodeOptions
) => TNativeAudioWorkletNode;
