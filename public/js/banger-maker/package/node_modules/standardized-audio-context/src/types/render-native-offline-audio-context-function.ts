import { TNativeAudioBuffer } from 'standardized-audio-context/src/types/native-audio-buffer';
import { TNativeOfflineAudioContext } from 'standardized-audio-context/src/types/native-offline-audio-context';

export type TRenderNativeOfflineAudioContextFunction = (
    nativeOfflineAudioContext: TNativeOfflineAudioContext
) => Promise<TNativeAudioBuffer>;
