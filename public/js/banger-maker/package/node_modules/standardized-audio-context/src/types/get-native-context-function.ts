import { IMinimalOfflineAudioContext, IOfflineAudioContext } from 'standardized-audio-context/src/interfaces/index';
import { TContext } from 'standardized-audio-context/src/types/context';
import { TNativeAudioContext } from 'standardized-audio-context/src/types/native-audio-context';
import { TNativeOfflineAudioContext } from 'standardized-audio-context/src/types/native-offline-audio-context';

export type TGetNativeContextFunction = <T extends TContext>(
    context: T
) => T extends IMinimalOfflineAudioContext | IOfflineAudioContext ? TNativeOfflineAudioContext : TNativeAudioContext;
