import { TNativeAudioDestinationNode } from 'standardized-audio-context/src/types/native-audio-destination-node';
import { TNativeContext } from 'standardized-audio-context/src/types/native-context';

export type TNativeAudioDestinationNodeFactory = (
    nativeContext: TNativeContext,
    channelCount: number,
    isNodeOfNativeOfflineAudioContext: boolean
) => TNativeAudioDestinationNode;
