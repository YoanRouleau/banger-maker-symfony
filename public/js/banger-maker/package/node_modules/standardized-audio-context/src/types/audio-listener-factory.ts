import { IAudioListener } from 'standardized-audio-context/src/interfaces/index';
import { TContext } from 'standardized-audio-context/src/types/context';
import { TNativeContext } from 'standardized-audio-context/src/types/native-context';

export type TAudioListenerFactory = <T extends TContext>(context: T, nativeContext: TNativeContext) => IAudioListener;
