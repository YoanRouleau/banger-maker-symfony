import { IAudioNodeRenderer, IMinimalOfflineAudioContext, IOfflineAudioContext, IStereoPannerNode } from 'standardized-audio-context/src/interfaces/index';

export type TStereoPannerNodeRendererFactory = <T extends IMinimalOfflineAudioContext | IOfflineAudioContext>() => IAudioNodeRenderer<
    T,
    IStereoPannerNode<T>
>;
