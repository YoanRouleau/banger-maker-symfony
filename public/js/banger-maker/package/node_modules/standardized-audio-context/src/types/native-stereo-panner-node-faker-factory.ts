import { INativeStereoPannerNodeFaker, IStereoPannerOptions } from 'standardized-audio-context/src/interfaces/index';
import { TNativeContext } from 'standardized-audio-context/src/types/native-context';

export type TNativeStereoPannerNodeFakerFactory = (
    nativeContext: TNativeContext,
    options: IStereoPannerOptions
) => INativeStereoPannerNodeFaker;
