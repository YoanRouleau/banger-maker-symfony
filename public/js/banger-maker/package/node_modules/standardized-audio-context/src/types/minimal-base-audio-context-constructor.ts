import { IMinimalBaseAudioContext } from 'standardized-audio-context/src/interfaces/index';
import { TContext } from 'standardized-audio-context/src/types/context';
import { TNativeContext } from 'standardized-audio-context/src/types/native-context';

export type TMinimalBaseAudioContextConstructor = new <T extends TContext>(
    nativeContext: TNativeContext,
    numberOfChannels: number
) => IMinimalBaseAudioContext<T>;
