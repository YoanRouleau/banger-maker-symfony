import { IAudioContext, IMinimalAudioContext } from 'standardized-audio-context/src/interfaces/index';
import { TNativeAudioContext } from 'standardized-audio-context/src/types/native-audio-context';

export type TIsAnyAudioContextFunction = (anything: unknown) => anything is IAudioContext | IMinimalAudioContext | TNativeAudioContext;
