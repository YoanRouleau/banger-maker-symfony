import { IIIRFilterOptions } from 'standardized-audio-context/src/interfaces/index';
import { TNativeContext } from 'standardized-audio-context/src/types/native-context';
import { TNativeIIRFilterNode } from 'standardized-audio-context/src/types/native-iir-filter-node';

export type TNativeIIRFilterNodeFactory = (
    nativeContext: TNativeContext,
    baseLatency: null | number,
    options: IIIRFilterOptions
) => TNativeIIRFilterNode;
