import { IReadOnlyMap } from 'standardized-audio-context/src/interfaces/index';
import { TNativeAudioParam } from 'standardized-audio-context/src/types/native-audio-param';

// @todo Since there are no native types yet they need to be crafted.
export type TNativeAudioParamMap = IReadOnlyMap<string, TNativeAudioParam>;
