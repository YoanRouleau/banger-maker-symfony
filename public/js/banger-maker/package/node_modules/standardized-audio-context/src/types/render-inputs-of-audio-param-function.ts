import { IAudioNode, IAudioParam, IMinimalOfflineAudioContext, IOfflineAudioContext } from 'standardized-audio-context/src/interfaces/index';
import { TNativeAudioParam } from 'standardized-audio-context/src/types/native-audio-param';
import { TNativeOfflineAudioContext } from 'standardized-audio-context/src/types/native-offline-audio-context';

export type TRenderInputsOfAudioParamFunction = <T extends IMinimalOfflineAudioContext | IOfflineAudioContext>(
    audioParam: IAudioParam,
    nativeOfflineAudioContext: TNativeOfflineAudioContext,
    nativeAudioParam: TNativeAudioParam,
    trace: readonly IAudioNode<T>[]
) => Promise<void>;
