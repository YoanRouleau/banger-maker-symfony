import { IChannelMergerOptions } from 'standardized-audio-context/src/interfaces/index';
import { TNativeChannelMergerNode } from 'standardized-audio-context/src/types/native-channel-merger-node';
import { TNativeContext } from 'standardized-audio-context/src/types/native-context';

export type TNativeChannelMergerNodeFactory = (nativeContext: TNativeContext, options: IChannelMergerOptions) => TNativeChannelMergerNode;
