import { IIIRFilterOptions, INativeIIRFilterNodeFaker } from 'standardized-audio-context/src/interfaces/index';
import { TNativeContext } from 'standardized-audio-context/src/types/native-context';

export type TNativeIIRFilterNodeFakerFactory = (
    nativeContext: TNativeContext,
    baseLatency: null | number,
    options: IIIRFilterOptions
) => INativeIIRFilterNodeFaker;
