import { TNativeContext } from 'standardized-audio-context/src/types/native-context';
import { TNativeScriptProcessorNode } from 'standardized-audio-context/src/types/native-script-processor-node';

export type TNativeScriptProcessorNodeFactory = (
    nativeContext: TNativeContext,
    bufferSize: number,
    numberOfInputChannels: number,
    numberOfOutputChannels: number
) => TNativeScriptProcessorNode;
