import { IAudioContextOptions, IMinimalAudioContext } from 'standardized-audio-context/src/interfaces/index';

export type TMinimalAudioContextConstructor = new (options?: IAudioContextOptions) => IMinimalAudioContext;
