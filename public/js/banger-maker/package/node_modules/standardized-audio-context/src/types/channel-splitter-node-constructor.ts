import { IAudioNode, IChannelSplitterOptions } from 'standardized-audio-context/src/interfaces/index';
import { TContext } from 'standardized-audio-context/src/types/context';

export type TChannelSplitterNodeConstructor = new <T extends TContext>(
    context: T,
    options?: Partial<IChannelSplitterOptions>
) => IAudioNode<T>;
