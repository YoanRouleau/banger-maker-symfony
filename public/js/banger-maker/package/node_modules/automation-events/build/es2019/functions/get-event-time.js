import { isCancelAndHoldAutomationEvent } from 'automation-events/build/es2019/guards/cancel-and-hold-automation-event';
import { isCancelScheduledValuesAutomationEvent } from 'automation-events/build/es2019/guards/cancel-scheduled-values-automation-event';
import { isExponentialRampToValueAutomationEvent } from 'automation-events/build/es2019/guards/exponential-ramp-to-value-automation-event';
import { isLinearRampToValueAutomationEvent } from 'automation-events/build/es2019/guards/linear-ramp-to-value-automation-event';
export const getEventTime = (automationEvent) => {
    if (isCancelAndHoldAutomationEvent(automationEvent) || isCancelScheduledValuesAutomationEvent(automationEvent)) {
        return automationEvent.cancelTime;
    }
    if (isExponentialRampToValueAutomationEvent(automationEvent) || isLinearRampToValueAutomationEvent(automationEvent)) {
        return automationEvent.endTime;
    }
    return automationEvent.startTime;
};
//# sourceMappingURL=get-event-time.js.map